@model IEnumerable<Warehouse.Models.Basket>
@{
    ViewData["Title"] = "Index";
}

@*<table class="table table-bordered table-hover">
        <thead class="bg-primary">
            <tr>
                <th>Name</th>
                <th>Count</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @{if (Model.Count()>0 && Model!=null)
                {

                    foreach (var pb in Model)
                    {
                    <tr>
                        <td>@pb.Product.Name</td>
                        <td>@pb.Count</td>
                        <td><a asp-action="Delete" asp-route-id="@pb.Id">Delete</a></td>
                    </tr>
                    }
                }
                else
                {
                    <p>No products to show!</p>
                }

                }
        </tbody>
    </table>*@
<partial name='@Url.Action("_IndexForHover", "Basket")' />
@*@Html.Partial("_IndexForHover");*@


